import ConnectWallet from './ConnectWallet'

const Header = () => {
    return (
        <nav className="text-white h-16 flex items-center justify-between px-4 md:px-10 mt-8">
            
        </nav>
    )
}

export default Header